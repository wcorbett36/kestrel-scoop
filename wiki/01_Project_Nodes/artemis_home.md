---
tags:
  - project
  - external
  - ingest
aliases:
  - artemis_home
last_updated: 2026-04-05
---

# artemis_home

**Ingest target** for kestrel-scoop (`config.yaml` project `artemis_home` → `../../artemis_home`). Content here may be **synthesized** from that tree when you run the K-Compiler; this note is a **vault stub** for linking.

If `artemis_home` does not exist beside your `artemis` workspace, expect the crawler to skip or omit that project until paths are aligned.

## Relationship

- Not the same as the **`artemis`** folder that contains `kestrel-systems/` and `trust-systems/`—naming is easy to confuse; this note refers to the **configured sibling** path only.

## See also

- [[01_Project_Nodes/kestrel-scoop]]
- [[00_Home]]
